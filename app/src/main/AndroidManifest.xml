<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunag.medicinetime">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity

            android:name=".login"
            android:exported="false" />
        <activity
            android:name=".medicine.MainActivity"

            android:exported="false" />
        <activity
            android:name=".medicine.quiz"
            android:exported="false" />
        <activity
            android:name=".medicine.chatBot"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".medicine.Register" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".medicine.MedicineActivity"
            android:exported="false" />
        <activity android:name="Registration">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".MainActivity" /> -->
        <activity android:name=".addmedicine.AddMedicineActivity" />
        <activity android:name=".alarm.ReminderActivity" />
        <activity android:name=".report.MonthlyReportActivity" />
    </application>

</manifest>